# PRD: Attack2Patch

- 상태: DRAFT
- 작성일: 2026-08-10
- 담당 도메인: Types / Config / Repo / Service / Runtime / UI
- 관련 문서:
  - `ARCHITECTURE.md`
  - `docs/design-docs/attack2patch-design.md`
  - `docs/SECURITY.md`
  - `docs/RELIABILITY.md`
  - `docs/FRONTEND.md`
- 원본:
  - Attack2Patch 기획 대화
  - `https://github.com/maxoverpro/harness/blob/master/docs/product-specs/PRD.md`

> Attack2Patch는 웹 공격 로그에서 공격받은 API를 식별하고, 원인이 된 취약 코드를 찾아 패치를 생성·검증한 뒤, 사용자 승인 후 Docker 이미지 재빌드와 재배포까지 수행하는 보안 자동화 제품이다.
>
> 이 문서는 3일 이내 구현 가능한 데모용 MVP 범위를 정의한다. 이 문서에 기록되지 않은 기능은 MVP 범위에 포함되지 않는다.

## 1. 배경

### 해결하려는 문제

웹 공격이 탐지된 이후 실제 대응까지는 다음과 같은 수작업이 필요하다.

1. 공격 로그 분석
2. 공격받은 API 식별
3. API와 관련된 소스 코드 탐색
4. 취약 코드 확인
5. 보안 패치 작성
6. 기능 및 보안 테스트
7. Docker 이미지 재생성
8. 서비스 재배포
9. 패치 효과 확인

공격 탐지 도구와 개발·배포 도구가 분리되어 있어 탐지부터 복구까지 시간이 오래 걸리고, 대응 과정에서 실수가 발생할 수 있다.

### 왜 지금인가

- 기존 탐지 도구는 공격 또는 취약점의 위치를 알려주지만, 실제 패치와 재배포는 개발자가 수행해야 한다.
- 코드 분석, 패치 제안 및 CI/CD 작업을 하나의 승인 기반 워크플로로 연결할 수 있다.
- 3일 MVP에서는 SQL Injection과 Flask 애플리케이션으로 범위를 제한해 전체 대응 과정을 재현할 수 있다.

### 성공 지표

- 준비된 SQL Injection 공격 탐지율 100%
- 공격 API와 취약 코드 매핑 성공률 100%
- 지원 코드 패턴에 대한 패치 생성 성공률 100%
- 패치 후 동일 공격 차단율 100%
- 패치 후 정상 API 기능 유지율 100%
- Docker 재배포 성공률 100%
- 배포 실패 시 롤백 성공률 100%
- 공격 탐지부터 재배포 완료까지 5분 이내

위 지표는 사전에 정의한 MVP 데모 시나리오를 기준으로 한다.

## 2. 목표 / 비목표

### 목표 (Goals)

- HTTP 요청 로그에서 SQL Injection 공격을 5초 이내에 탐지한다.
- 공격받은 Flask API와 관련 소스 파일 및 함수 위치를 식별한다.
- 문자열 결합 방식의 SQL 쿼리를 파라미터 바인딩 방식으로 수정한다.
- 패치 전후 코드 Diff와 변경 이유를 제공한다.
- 정상 기능 테스트와 SQL Injection 재현 테스트를 자동으로 수행한다.
- 검증된 코드로 새로운 Docker 이미지를 빌드한다.
- 사용자 승인 후 Docker Compose 환경에 새 컨테이너를 배포한다.
- 배포 검증 실패 시 이전 정상 이미지로 롤백한다.
- 탐지, 패치, 테스트, 배포 결과를 웹 대시보드에서 확인할 수 있게 한다.

### 비목표 (Non-Goals)

- SQL Injection 이외의 취약점 탐지 및 패치
- Flask 이외의 프레임워크 지원
- Python 이외의 언어 지원
- Kubernetes 및 다중 서버 배포
- AWS, GCP, Azure 등 클라우드 플랫폼 연동
- WAF, SIEM 또는 상용 보안 제품 연동
- 운영 환경에서의 무승인 완전 자동 배포
- 임의 형태의 취약 코드를 모두 수정하는 범용 AI 패치
- GitHub Pull Request 자동 생성
- 무중단 Blue/Green 또는 Canary 배포
- 실제 고객 데이터 처리

## 3. 사용자 / 페르소나

| 페르소나 | 니즈 | 비고 |
| --- | --- | --- |
| 애플리케이션 개발자 | 공격받은 API와 취약 코드를 빠르게 찾고 패치를 검토하고 싶다 | 주요 사용자 |
| 보안 담당자 | 실제 공격과 코드 취약점의 연관성 및 패치 효과를 확인하고 싶다 | 공격·보안 검증 담당 |
| 운영 담당자 | 검증된 이미지를 배포하고 실패 시 이전 버전으로 복구하고 싶다 | 배포 결과 확인 |
| 데모 평가자 | 공격부터 패치와 재배포까지의 흐름을 한 화면에서 확인하고 싶다 | MVP 시연 대상 |

## 4. 요구사항

### 4.1 기능 요구사항 (FR)

| ID | 요구사항 | 우선순위 | 수용 기준 |
| --- | --- | --- | --- |
| FR-1 | 시스템은 HTTP 요청 로그를 수집해야 한다 | P0 | 시간, 메서드, 경로, 파라미터, 출발지 IP, 상태 코드를 포함한 로그를 파싱한다 |
| FR-2 | 시스템은 SQL Injection 공격 패턴을 탐지해야 한다 | P0 | 준비된 5개 공격 페이로드를 모두 탐지하고 정상 요청을 공격으로 분류하지 않는다 |
| FR-3 | 탐지 결과에 공격 근거를 표시해야 한다 | P0 | 공격 유형, 위험도, 요청 경로, 탐지된 문자열을 화면에 표시한다 |
| FR-4 | 공격 경로를 Flask 라우트와 연결해야 한다 | P0 | `/api/users` 요청을 대응하는 파일과 함수에 정확히 매핑한다 |
| FR-5 | SQL 문자열에 사용자 입력이 직접 결합되는 코드를 찾아야 한다 | P0 | 파일 경로, 함수 이름, 줄 번호 및 취약 코드를 표시한다 |
| FR-6 | 안전한 패치 후보를 생성해야 한다 | P0 | 문자열 결합 SQL을 SQLite 파라미터 바인딩 방식으로 변경한다 |
| FR-7 | 패치 전후 Diff를 제공해야 한다 | P0 | 변경 파일과 추가·삭제된 코드가 구분되어 표시된다 |
| FR-8 | 사용자가 패치 적용 여부를 승인할 수 있어야 한다 | P0 | 승인 전 원본 소스와 실행 중인 컨테이너가 변경되지 않는다 |
| FR-9 | 패치 코드에 대한 자동 검증을 수행해야 한다 | P0 | 구문 검사, 단위 테스트, 정상 요청 테스트, 공격 재현 테스트를 실행한다 |
| FR-10 | 패치 이후 취약점을 재검사해야 한다 | P0 | 동일 정적 분석 규칙에서 취약 코드가 다시 발견되지 않는다 |
| FR-11 | 검증된 소스로 Docker 이미지를 빌드해야 한다 | P0 | 고유 태그를 가진 이미지가 생성되고 빌드 로그가 저장된다 |
| FR-12 | 배포 전 사용자 승인을 받아야 한다 | P0 | 모든 필수 검증 통과 전에는 배포 승인 버튼이 비활성화된다 |
| FR-13 | 승인된 이미지를 Docker Compose로 재배포해야 한다 | P0 | 새 이미지 태그를 사용하는 컨테이너가 정상 실행된다 |
| FR-14 | 배포 후 헬스체크를 수행해야 한다 | P0 | `/health`가 제한 시간 내 HTTP 200을 반환한다 |
| FR-15 | 배포 후 정상 요청과 공격 요청을 다시 실행해야 한다 | P0 | 정상 요청은 성공하고 SQL Injection 요청으로 비인가 데이터가 노출되지 않는다 |
| FR-16 | 배포 검증 실패 시 이전 버전으로 롤백해야 한다 | P0 | 기존 이미지로 컨테이너가 복구되고 `/health`가 HTTP 200을 반환한다 |
| FR-17 | 전체 처리 상태를 대시보드에 표시해야 한다 | P1 | 탐지, 코드 분석, 패치, 테스트, 빌드, 배포 상태가 단계별로 표시된다 |
| FR-18 | 처리 이력을 저장하고 조회할 수 있어야 한다 | P1 | 이벤트별 공격 정보, 패치, 테스트 결과, 이미지 태그, 배포 결과를 조회할 수 있다 |
| FR-19 | 사용자가 수동 롤백을 요청할 수 있어야 한다 | P1 | 최근 정상 이미지로 복구하고 결과를 기록한다 |
| FR-20 | 실패 원인을 사용자에게 제공해야 한다 | P1 | 코드 매핑, 패치, 테스트, 빌드 또는 배포 실패 단계와 원인이 표시된다 |

### 4.2 비기능 요구사항 (NFR)

| ID | 분류 | 요구사항 및 수용 기준 |
| --- | --- | --- |
| NFR-1 | 성능 | 로그 입력 후 공격 탐지 결과를 5초 이내에 표시한다 |
| NFR-2 | 성능 | 코드 탐지와 패치 후보 생성을 30초 이내에 완료한다 |
| NFR-3 | 성능 | 탐지부터 재배포 검증까지 5분 이내에 완료한다 |
| NFR-4 | 안정성 | 이미지 빌드 실패가 현재 실행 중인 컨테이너에 영향을 주지 않는다 |
| NFR-5 | 안정성 | 배포 검증 실패 시 60초 이내에 이전 이미지로 롤백한다 |
| NFR-6 | 보안 | 패치와 배포는 반드시 명시적인 사용자 승인을 거친다 |
| NFR-7 | 보안 | 쿠키, 인증 헤더, 토큰 및 비밀번호는 로그 저장 전에 마스킹한다 |
| NFR-8 | 보안 | HTTP 입력값을 셸 명령, 이미지 태그 또는 파일 경로에 직접 삽입하지 않는다 |
| NFR-9 | 보안 | Docker 제어 인터페이스는 외부 네트워크에 노출하지 않는다 |
| NFR-10 | 보안 | 패치 엔진은 허용된 작업 디렉터리와 파일만 수정할 수 있다 |
| NFR-11 | 보안 | 패치된 코드는 테스트 및 보안 재검사를 통과하기 전 배포할 수 없다 |
| NFR-12 | 가용성 | MVP는 단일 서버 Docker Compose 환경에서 동작한다 |
| NFR-13 | 추적성 | 모든 상태 변경에 이벤트 ID, 시간, 결과 및 오류를 기록한다 |
| NFR-14 | 재현성 | 동일 소스와 설정으로 동일한 Docker 이미지를 다시 빌드할 수 있어야 한다 |

세부 보안 기준은 `docs/SECURITY.md`, 배포 및 복구 기준은 `docs/RELIABILITY.md`를 기록 시스템으로 사용한다.

### 4.3 데이터 요구사항

#### AttackEvent

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| id | UUID | 예 | 공격 이벤트 식별자 |
| detected_at | DateTime | 예 | 탐지 시각 |
| source_ip | String | 예 | 마스킹 가능한 출발지 IP |
| method | Enum | 예 | HTTP 메서드 |
| path | String | 예 | 요청 경로 |
| sanitized_payload | String | 예 | 민감정보가 제거된 공격 입력 |
| attack_type | Enum | 예 | MVP에서는 `SQL_INJECTION` |
| severity | Enum | 예 | LOW / MEDIUM / HIGH / CRITICAL |
| status | Enum | 예 | 처리 상태 |
| evidence | JSON | 예 | 공격으로 판단한 근거 |

#### CodeFinding

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| id | UUID | 예 | 코드 탐지 결과 식별자 |
| event_id | UUID | 예 | 공격 이벤트 참조 |
| file_path | String | 예 | 저장소 내부 상대 경로 |
| function_name | String | 예 | 취약 함수 |
| line_number | Integer | 예 | 취약 코드 시작 줄 |
| rule_id | String | 예 | 탐지 규칙 ID |
| vulnerable_code | String | 예 | 취약 코드 일부 |

#### PatchCandidate

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| id | UUID | 예 | 패치 식별자 |
| finding_id | UUID | 예 | 코드 탐지 결과 참조 |
| diff | Text | 예 | 패치 Diff |
| status | Enum | 예 | GENERATED / APPROVED / REJECTED / VALIDATED |
| validation_result | JSON | 아니요 | 테스트 및 재검사 결과 |

#### Deployment

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| id | UUID | 예 | 배포 식별자 |
| patch_id | UUID | 예 | 패치 참조 |
| previous_image | String | 예 | 롤백 대상 이미지 |
| candidate_image | String | 예 | 신규 이미지 |
| status | Enum | 예 | BUILDING / DEPLOYING / COMPLETED / FAILED / ROLLED_BACK |
| healthcheck_result | JSON | 아니요 | 배포 후 검증 결과 |
| deployed_at | DateTime | 아니요 | 배포 완료 시각 |

구체적인 스키마는 `docs/generated/db-schema.md`에 기록한다.

모든 외부 입력은 경계에서 정의된 스키마로 파싱한다. 알 수 없는 필드, 잘못된 타입 및 허용되지 않은 경로는 거부한다.

### 4.4 UI/UX 요구사항

#### 공격 이벤트 목록

- 탐지 시각
- 공격 유형
- 대상 API
- 위험도
- 처리 상태
- 상세 화면 이동

#### 공격 및 코드 상세 화면

- 마스킹된 공격 요청
- 공격 탐지 근거
- 대상 API
- 소스 파일과 함수
- 취약 코드 위치
- 패치 전후 Diff
- 패치 승인 또는 거절

#### 검증 화면

- Python 구문 검사
- 단위 테스트
- 정상 요청 테스트
- SQL Injection 재현 테스트
- 정적 분석 재검사
- 각 검증의 성공·실패 및 오류 메시지

#### 배포 화면

- 이전 이미지 태그
- 신규 이미지 태그
- 빌드 상태
- 배포 상태
- 헬스체크
- 재공격 테스트
- 롤백 상태

#### 검증 가능성

- 주요 상태에는 고정된 `data-testid`를 부여한다.
- 승인 버튼은 검증 상태에 따라 활성화 여부를 DOM에서 확인할 수 있어야 한다.
- 성공, 실패, 롤백 상태를 DOM 스냅샷으로 구분할 수 있어야 한다.
- UI 규약은 `docs/FRONTEND.md`에 정의한다.

## 5. 사용자 여정 (User Journey)

### 여정 1: 공격 탐지 및 취약 코드 확인

1. 취약한 Flask 애플리케이션이 실행 중이다.
2. 사용자가 정상 사용자 조회 API를 호출한다.
3. 공격자가 SQL Injection 페이로드를 전송한다.
4. 시스템이 요청 로그에서 SQL Injection을 탐지한다.
5. 대시보드에 새로운 공격 이벤트가 5초 이내에 표시된다.
6. 사용자가 이벤트를 선택한다.
7. 공격받은 API, 관련 소스 파일, 함수와 취약 코드가 표시된다.

### 여정 2: 패치 생성 및 검증

1. 사용자가 취약점 상세 화면에서 패치 생성을 요청한다.
2. 시스템이 파라미터 바인딩 방식의 패치를 생성한다.
3. 사용자가 변경 전후 Diff와 변경 이유를 확인한다.
4. 사용자가 패치를 승인한다.
5. 시스템이 격리된 작업 복사본에 패치를 적용한다.
6. 구문 검사, 단위 테스트, 정상 요청 및 공격 재현 테스트를 수행한다.
7. 모든 검사 결과가 성공으로 표시된다.
8. 실패한 검사가 있으면 배포 단계로 진행하지 않는다.

### 여정 3: Docker 이미지 생성 및 재배포

1. 모든 검증이 통과한 상태에서 배포 승인 버튼이 활성화된다.
2. 사용자가 배포를 승인한다.
3. 시스템이 패치된 소스로 고유 태그의 Docker 이미지를 빌드한다.
4. 기존 정상 이미지 태그를 롤백 대상으로 기록한다.
5. Docker Compose가 새 이미지로 컨테이너를 재생성한다.
6. `/health`, 정상 요청 및 SQL Injection 재공격 테스트를 수행한다.
7. 모든 검증이 성공하면 배포 상태를 `COMPLETED`로 변경한다.

### 여정 4: 배포 실패 및 롤백

1. 신규 컨테이너의 헬스체크 또는 기능 테스트가 실패한다.
2. 시스템이 신규 컨테이너를 중지한다.
3. 기존 정상 이미지로 컨테이너를 복구한다.
4. 복구된 서비스의 `/health` 응답을 확인한다.
5. 상태를 `ROLLED_BACK`으로 기록하고 실패 원인을 표시한다.

## 6. 수용 기준 (Acceptance Criteria)

### 공격 탐지와 코드 추적

- [ ] FR-1: HTTP 요청 로그를 정의된 스키마로 파싱한다.
- [ ] FR-2: 준비된 SQL Injection 페이로드 5개를 모두 탐지한다.
- [ ] FR-2: 정상 요청을 SQL Injection으로 오탐하지 않는다.
- [ ] FR-4: 공격 경로를 정확한 Flask 라우트에 연결한다.
- [ ] FR-5: 취약 파일, 함수 및 코드 줄을 표시한다.

### 패치 및 검증

- [ ] FR-6: 문자열 결합 SQL을 파라미터 바인딩으로 변경한다.
- [ ] FR-7: 변경 전후 Diff를 표시한다.
- [ ] FR-8: 사용자 승인 전 원본 소스를 변경하지 않는다.
- [ ] FR-9: 정상 API 테스트가 통과한다.
- [ ] FR-9: 동일 SQL Injection 공격으로 비인가 데이터가 노출되지 않는다.
- [ ] FR-10: 패치 후 정적 분석에서 동일 취약점이 발견되지 않는다.

### 이미지 빌드와 배포

- [ ] FR-11: 고유 태그의 Docker 이미지가 생성된다.
- [ ] FR-12: 검증 통과 전 배포 승인 버튼이 비활성화된다.
- [ ] FR-13: 승인 후 신규 이미지로 컨테이너가 실행된다.
- [ ] FR-14: 신규 컨테이너의 `/health`가 HTTP 200을 반환한다.
- [ ] FR-15: 정상 요청이 기존과 동일하게 동작한다.
- [ ] FR-15: SQL Injection 재공격이 차단된다.
- [ ] FR-16: 배포 검증 실패 시 기존 이미지로 롤백된다.

### 비기능 및 리뷰

- [ ] NFR-1: 공격 로그 입력 후 5초 이내에 탐지 결과가 표시된다.
- [ ] NFR-3: 전체 데모 흐름이 5분 이내에 완료된다.
- [ ] NFR-7: 저장된 로그에서 인증정보와 민감정보가 마스킹된다.
- [ ] NFR-9: Docker 제어 인터페이스가 외부에 노출되지 않는다.
- [ ] 보안 리뷰에서 P0 또는 P1 취약점이 발견되지 않는다.
- [ ] 아키텍처 리뷰에서 레이어 의존성 위반이 발견되지 않는다.
- [ ] `scripts/check.sh` 전체 검증이 통과한다.
- [ ] `docs/QUALITY_SCORE.md`에서 MVP 핵심 도메인이 B 이상이다.

## 7. 오픈 질문 / 리스크

| 질문/리스크 | 영향 | 상태 |
| --- | --- | --- |
| 패치 엔진을 규칙 기반으로만 구현할 것인가 | LLM 사용 시 결과 편차와 검증 부담이 증가함 | 규칙 기반으로 결정 |
| Docker 제어 방식을 어떻게 제한할 것인가 | Docker 소켓 접근 권한이 크면 호스트 위험으로 이어질 수 있음 | 전용 배포 모듈과 허용 명령 목록 사용 |
| Flask 라우트 매핑이 실패할 수 있음 | 공격과 코드의 연결이 중단됨 | 지원 프로젝트 구조를 고정 |
| 패치 후 정상 기능이 변경될 수 있음 | 잘못된 패치가 배포될 수 있음 | 정상 요청 회귀 테스트 필수 |
| 이미지 빌드가 5분 목표를 초과할 수 있음 | 데모 시간이 길어짐 | 작은 베이스 이미지와 빌드 캐시 사용 |
| 서비스 중단이 발생할 수 있음 | 재배포 중 짧은 다운타임 발생 | MVP에서는 허용, 화면에 상태 표시 |
| 공격 로그에 민감정보가 포함될 수 있음 | 데이터 노출 위험 | 저장 전 마스킹 |
| 실제 운영 환경으로 오인될 수 있음 | MVP 안전성 수준에 대한 과대평가 | 데모 전용임을 README와 UI에 표시 |

## 8. 결정 기록 (ADR)

- 2026-08-10: 제품명을 `Attack2Patch`로 결정했다. 공격 탐지부터 패치와 재배포까지의 연결을 표현하기 위함이다.
- 2026-08-10: 3일 MVP의 대상 언어와 프레임워크를 Python Flask로 제한했다.
- 2026-08-10: MVP 지원 취약점을 SQL Injection 하나로 제한했다.
- 2026-08-10: 패치는 임의 AI 생성보다 예측 가능한 규칙 기반 변환을 우선한다.
- 2026-08-10: 배포 환경은 단일 서버의 Docker Compose로 제한한다.
- 2026-08-10: 패치 적용과 재배포에는 각각 사용자의 명시적 승인이 필요하다.
- 2026-08-10: 배포 후 헬스체크, 정상 요청 및 공격 재현 테스트를 필수 게이트로 지정한다.
- 2026-08-10: 배포 검증 실패 시 이전 정상 이미지로 자동 롤백한다.
- 2026-08-10: MVP에서는 짧은 재배포 중단 시간을 허용하고 무중단 배포는 비목표로 둔다.

## 9. 관련 산출물

- 실행 계획: `docs/exec-plans/active/attack2patch-mvp.md`
- 완료된 실행 계획: `docs/exec-plans/completed/`
- 설계 문서: `docs/design-docs/attack2patch-design.md`
- 위협 모델: `docs/design-docs/attack2patch-threat-model.md`
- 제품 요구사항: `docs/product-specs/attack2patch-prd.md`
- 데이터 스키마: `docs/generated/db-schema.md`
- API 명세: `docs/generated/openapi.md`
- 공격 규칙: `docs/references/sql-injection-rules.md`
- 보안 기준: `docs/SECURITY.md`
- 안정성 및 롤백 기준: `docs/RELIABILITY.md`
- 프론트엔드 규약: `docs/FRONTEND.md`
- 품질 등급: `docs/QUALITY_SCORE.md`
