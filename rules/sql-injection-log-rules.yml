rules:
  - id: sql-injection-log-pattern
    description: Detect representative SQL Injection text in sanitized HTTP logs
    patterns:
      - "(?i)'\\s*or\\s+"
      - "(?i)union\\s+select"
      - "(?i)(sleep|benchmark)\\s*\\("
